// NOTE: i18n middleware disabled in Phase 1
// Will be re-enabled in Phase 2 after resolving next-intl ESM/CommonJS conflicts
// For now, dashboard runs in English only

export const config = {
  matcher: [
    // Disable middleware for now - Phase 1 uses English only
    '/((?!_next|_vercel|.*\\.).*)',
  ],
}

// Identity middleware that does nothing
export default function middleware() {
  // Placeholder for Phase 2 i18n support
}
